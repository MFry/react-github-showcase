/**
 * Styled text block that denotes code
 */
import styled from 'styled-components';

const Code = styled.code`
  code {
    font-family: source-code-pro, Menlo, Monaco, Consolas, 'Courier New',
      monospace;
  }
`;

export default Code;
