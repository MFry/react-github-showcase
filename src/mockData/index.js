/*
 * Point of entry for mock data
 * All the mock data is set to be immutable
 */

import events from './events.json';
import repos from './repos.json';
import user from './user.json';

export { events, repos, user };
